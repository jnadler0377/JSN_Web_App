# requirements.txt - Updated with new features

# ========================================
# CORE DEPENDENCIES (Existing)
# ========================================
fastapi==0.104.1
uvicorn[standard]==0.24.0
jinja2==3.1.2
python-multipart==0.0.6
sqlalchemy==2.0.23
requests==2.31.0

# ========================================
# NEW: Configuration Management (Feature 17)
# ========================================
pydantic-settings==2.1.0
python-dotenv==1.0.0

# ========================================
# NEW: Authentication & Security (Feature 9)
# ========================================
bcrypt==4.1.2
passlib==1.7.4

# ========================================
# NEW: Background Jobs (Feature 16)
# ========================================
# Optional - Install only if enabling Celery
celery==5.3.4
redis==5.0.1

# ========================================
# NEW: OCR & Document Processing (Feature 11)
# ========================================
# Optional - Install only if enabling OCR
PyPDF2==3.0.1
pytesseract==0.3.10       # Requires tesseract-ocr system package
pdf2image==1.16.3         # Requires poppler system package
# boto3==1.34.1           # If using AWS Textract instead of tesseract

# ========================================
# EXISTING DEPENDENCIES
# ========================================
beautifulsoup4==4.12.2
selenium==4.15.2
webdriver-manager==4.0.1

# ========================================
# UTILITIES
# ========================================
aiofiles==23.2.1          # For async file operations

# ========================================
# DEVELOPMENT DEPENDENCIES (Optional)
# ========================================
# pytest==7.4.3
# pytest-asyncio==0.21.1
# black==23.12.0
# flake8==6.1.0
# mypy==1.7.1

# ========================================
# SYSTEM REQUIREMENTS
# ========================================
# The following must be installed at the system level:
#
# For OCR:
#   - tesseract-ocr (Ubuntu: apt-get install tesseract-ocr)
#   - poppler-utils (Ubuntu: apt-get install poppler-utils)
#
# For Redis (if using Celery or caching):
#   - Redis server (Ubuntu: apt-get install redis-server)
#
# For ChromeDriver (existing scraper):
#   - Google Chrome
#   - ChromeDriver (auto-managed by webdriver-manager)
